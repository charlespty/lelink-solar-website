{
  "name": "lelink-solar-website",
  "version": "1.0.0",
  "description": "Lelink Solar - Professional solar generator solutions",
  "private": true,
  "scripts": {
    "build": "cd Website && npm ci --only=production && npm run build",
    "start": "cd Website && npm start",
    "install": "cd Website && npm ci --only=production"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
